# SETTINGS_PAGE

Ordem: navegação de configuração → SectionHeader → FormSection/Configuration Cards → save bar sticky quando houver alterações. Sempre: salvar, cancelar, validação inline e erro abaixo do campo. Abas/contexto persistem por URL quando relevantes.
