# Candidate @richardrx post URLs to consider adding to the skill — one per line.
# Blank lines and # comments are ignored. `python update.py` fetches only the ones
# NOT already covered by the skill, so it's safe to leave old URLs here.
#
# Tip: run `python discover_new.py` to list recent posts not yet in the skill.
#
# https://x.com/richardrx/status/XXXXXXXXXXXXXXXXXXX
